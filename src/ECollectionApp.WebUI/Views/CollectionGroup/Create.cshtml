@model CollectionGroupViewModel

@{ Layout = null; }

<div class="row">
    <div class="col-md-12">
        <form asp-action="Create" asp-route-id="@Model.Group.Id" onsubmit="return jQueryAjaxPost(this)">
            @await Html.PartialAsync("_EditPartial", Model)
        </form>
    </div>
</div>